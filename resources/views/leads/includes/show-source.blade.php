<p class="uppercase text-xs text-gray-400">{{__('leads.table_source')}}</p>
<div class="flex flex-row w-full justify-between items-center">
    <p class="text-lg md:text-2xl">{{ getSourceName($lead->source_id) }}</p>
</div>